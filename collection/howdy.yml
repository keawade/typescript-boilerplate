info:
  name: /howdy
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/howdy"
  params:
    - name: name
      value: Bob
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
